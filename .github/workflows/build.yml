name: Build and Test

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  FLUTTER_VERSION: '3.35.5'
  PROJECT_NAME: 'Project-GG'

jobs:
<<<<<<< HEAD
=======

>>>>>>> 66430493cdc7e1b979a86ca6b24026a341676124
  lint:
    name: Lint and Analyze
    runs-on: ubuntu-latest
    outputs:
      quality: ${{ steps.analyze.outputs.quality }}
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Setup Flutter Manually
      run: |
        echo "Cloning Flutter repository for version ${{ env.FLUTTER_VERSION }}..."
        git clone https://github.com/flutter/flutter.git --depth 1 --branch ${{ env.FLUTTER_VERSION }} $HOME/flutter
        echo "$HOME/flutter/bin" >> $GITHUB_PATH
    
    - name: Verify Installation
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        echo "=== Flutter Version ==="
        flutter --version
        echo "=== Dart Version ==="
        dart --version
    
    - name: Install dependencies
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        flutter pub get
    
    - name: Run Flutter Analyze
      id: analyze
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        echo "Running Flutter analyze..."
<<<<<<< HEAD
        # НЕСТРОГИЙ РЕЖИМ
=======
        # СТАРЫЙ ВАРИАНТ (нестрогий) - просто показывает ошибки, но не падает
>>>>>>> 66430493cdc7e1b979a86ca6b24026a341676124
        flutter analyze lib/ || echo "⚠️ Linter found issues (non-fatal mode)"
        echo "quality=passed" >> $GITHUB_OUTPUT
    
    - name: Check Formatting
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        echo "Checking code formatting..."
<<<<<<< HEAD
        # НЕСТРОГИЙ РЕЖИМ
=======
        # СТАРЫЙ ВАРИАНТ - только показывает, какие файлы нужно отформатировать
>>>>>>> 66430493cdc7e1b979a86ca6b24026a341676124
        dart format --output=none lib/ || echo "⚠️ Some files need formatting"

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: lint
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Flutter Manually
      run: |
        echo "Cloning Flutter repository for version ${{ env.FLUTTER_VERSION }}..."
        git clone https://github.com/flutter/flutter.git --depth 1 --branch ${{ env.FLUTTER_VERSION }} $HOME/flutter
        echo "$HOME/flutter/bin" >> $GITHUB_PATH
    
    - name: Verify Installation
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        flutter --version
    
    - name: Install dependencies
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        flutter pub get
    
    - name: Run Tests
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        flutter test --coverage
    
    - name: Upload Coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage/lcov.info
        fail_ci_if_error: false

<<<<<<< HEAD
=======

>>>>>>> 66430493cdc7e1b979a86ca6b24026a341676124
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    needs: test
    if: |
      github.event_name == 'push' && 
      (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Flutter Manually
      run: |
        echo "Cloning Flutter repository for version ${{ env.FLUTTER_VERSION }}..."
        git clone https://github.com/flutter/flutter.git --depth 1 --branch ${{ env.FLUTTER_VERSION }} $HOME/flutter
        echo "$HOME/flutter/bin" >> $GITHUB_PATH
    
    - name: Verify Installation
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        flutter --version
    
    - name: Install dependencies
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        flutter pub get
    
    - name: Build APK
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        flutter build apk --release
    
    - name: Build App Bundle
      run: |
        export PATH="$HOME/flutter/bin:$PATH"
        flutter build appbundle --release
    
    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: build/app/outputs/flutter-apk/app-release.apk
        retention-days: 7
    
    - name: Upload App Bundle Artifact
      uses: actions/upload-artifact@v4
      with:
        name: android-appbundle
        path: build/app/outputs/bundle/release/app-release.aab
        retention-days: 7

<<<<<<< HEAD
=======

>>>>>>> 66430493cdc7e1b979a86ca6b24026a341676124
  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [build-android]
    if: startsWith(github.ref, 'refs/tags/v')
    environment: 
      name: release
      url: https://github.com/${{ github.repository }}/releases
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Download APK Artifact
        uses: actions/download-artifact@v4
        with:
          name: android-apk
      
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            app-release.apk
          draft: false
          prerelease: false
      
      - name: Log success
<<<<<<< HEAD
        run: |
          echo "✅ Release ${{ github.ref_name }} created successfully!"
          echo "📦 Download: https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}"
=======
        run: |
          echo "✅ Release ${{ github.ref_name }} created successfully!"
          echo "📦 Download: https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}"

      - name: Deploy Release APK to Server
        run: |
          # Копируем APK релиза на сервер (как в обычном деплое)
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          
          # Переименовываем APK с версией тега
          TAG_NAME="${{ github.ref_name }}"
          mv app-release.apk "app-${TAG_NAME}.apk"
          
          # Копируем на сервер
          scp -o ConnectTimeout=10 -i ~/.ssh/id_ed25519 "app-${TAG_NAME}.apk" ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/var/www/flutter_builds/
          
          echo "✅ Release APK deployed to server: app-${TAG_NAME}.apk"
>>>>>>> 66430493cdc7e1b979a86ca6b24026a341676124
