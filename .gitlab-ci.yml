# .gitlab-ci.yml - простейшая версия для проверки

stages:
  - test

test_pipeline:
  stage: test
  script:
    - echo "✅ CI/CD пайплайн работает!"
    - echo "Проект: $CI_PROJECT_NAME"
    - echo "Ветка: $CI_COMMIT_REF_NAME"
  tags:
    - docker