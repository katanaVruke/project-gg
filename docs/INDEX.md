# 📊 Документация CI/CD пайплайна
*Проект: Project-GG (Flutter приложение)*

## 🚀 Обзор системы
**GitHub Actions CI/CD пайплайн** для автоматической сборки, тестирования и деплоя Flutter приложения.

## 📋 Основные workflow

### 1. Основной пайплайн (flutter-ci.yml)
**Триггеры:**
- Push в ветки `main`, `develop`
- Pull request в `main`
- Ручной запуск

**Этапы:**
1. **lint** - Анализ кода (`flutter analyze`)
2. **test** - Запуск тестов (`flutter test`)
3. **build** - Сборка APK (`flutter build apk`)
4. **deploy** - Деплой (имитация SSH)
5. **notify** - Уведомления в Discord

### 2. Релизный пайплайн (release.yml)
**Триггер:** Создание тега `v*.*.*` (например: `v1.0.0`)

**Автоматически:**
- Создает релиз на GitHub
- Генерирует release notes из коммитов
- Прикрепляет APK файлы

## ⚙️ Настройка окружения

### Секреты GitHub:
| Секрет | Назначение | Обязательный |
|--------|------------|--------------|
| `DISCORD_WEBHOOK_URL` | Webhook для уведомлений | Нет |
| `SSH_PRIVATE_KEY` | SSH ключ для деплоя | Нет |

### Переменные:
```yaml
FLUTTER_VERSION: '3.13.9'   # Версия Flutter
PROJECT_NAME: 'Project-GG'  # Имя проекта