# 🚀 Описание пайплайна

## Workflow файлы

### 1. Основной пайплайн (.github/workflows/flutter-ci.yml)
**Назначение:** Полный цикл CI/CD

**Триггеры:**
- `push` в ветки `main`, `develop`
- `pull_request` в `main`
- `workflow_dispatch` (ручной запуск)

**Этапы выполнения:**
1. **lint** - Проверка кода (`flutter analyze`, `flutter format`)
2. **test** - Запуск тестов (`flutter test --coverage`)
3. **build** - Сборка APK и App Bundle
4. **deploy** - Деплой на сервер (имитация SSH)
5. **notify** - Отправка уведомлений в Discord

### 2. Релизный пайплайн (.github/workflows/release.yml)
**Назначение:** Автоматическое создание релизов

**Триггер:** `push` тегов вида `v*.*.*` (например `v1.0.0`)

**Что делает:**
- Создает релиз на GitHub
- Генерирует changelog из коммитов
- Прикрепляет собранные APK файлы